# Keep secrets in your existing secret store/.env.local, never commit them here.
PUBLIC_ORIGIN=https://learn.example.com
TRUST_PROXY=true
BIND_HOST=127.0.0.1
COOKIE_SECURE=true
HTTPS_ENABLED=false
PORT=4173
